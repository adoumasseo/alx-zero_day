#include <stdio.h>
/**
 * main - Entry point
 * description: print alphabet
 * Return: zero as succes
 */
int main(void)
{
	int i;

	for (n = 97; n <= 122; n++)
		putchar(n);
	putchar(10);
	return (0);
}
