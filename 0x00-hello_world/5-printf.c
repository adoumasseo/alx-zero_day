#include <stdio.h>

/**
 * main - entry point
 * Description: it's about print string using prinf()
 * Return: zero as succes
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
