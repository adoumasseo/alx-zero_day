#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: it's first parameter
 * @b: it's second
 *
 * Description: a function that multiplies two integers.
 * Return: multiplication of two parameter
 */

int mul(int a, int b)
{
	return (a * b);
}
